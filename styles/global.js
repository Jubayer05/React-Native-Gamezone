import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  container: {
    padding: 30,
  },

  titleText: {
    fontSize: 24,
    color: "#555",
  },

  paragraph: { marginVertical: 8, lineHeight: 22, color: "#555" },
});
